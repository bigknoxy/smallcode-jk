{
  "name": "smallcode",
  "version": "1.1.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "smallcode": "bin/smallcode.ts"
  },
  "scripts": {
    "test": "bun test",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun run lint && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/bun": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
