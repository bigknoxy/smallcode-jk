{
  "id": "edit-rel-bigfile-countcols_1",
  "desc": "Fix the wrong-comparison bug in `countColumns` inside the large file src/table.ts. The running maximum uses `row.length < max` instead of `>`, so the loop never grows `max` and the function returns the wrong column count (0) for ragged rows. Edit only the faulty function so tests/table.test.ts passes. The other modules already pass.",
  "setup": { "repoFixture": "edit-rel-bigfile-countcols_1" },
  "graders": [
    {
      "type": "deterministic_tests",
      "required": ["tests/table.test.ts"],
      "command": "bun test"
    }
  ],
  "tracked_metrics": ["n_turns", "n_toolcalls", "n_total_tokens", "pass_at_1"],
  "tags": ["edit-reliability", "bigfile", "wrong-comparison"],
  "reference_solution": "edit-rel-bigfile-countcols_1-solution/"
}
