{
  "id": "multifile-receipt_1",
  "desc": "In this small library, receiptTotal(items) should sum each line item's priceCents × qty and return the grand total formatted as USD with two decimal places (e.g. \"$6.00\"). It currently returns the wrong value. Two things are broken on this code path, in different source files under src/. Find and fix both so tests/receipt.test.ts passes.",
  "setup": { "repoFixture": "multifile-receipt_1" },
  "graders": [{ "type": "deterministic_tests", "required": ["tests/receipt.test.ts"], "command": "bun test" }],
  "tracked_metrics": ["n_turns", "n_toolcalls", "n_total_tokens", "pass_at_1"],
  "tags": ["multifile", "coordination", "genuine-multifile"],
  "reference_solution": "multifile-receipt_1-solution/"
}
