{
  "id": "cap-already-implements_1",
  "desc": "A working `unique<T>(arr: T[]): T[]` function that already uses a Set internally — do not rewrite it, the implementation is correct and tests already pass",
  "setup": {
    "files": {
      "src/unique.ts": "/**\n * Returns a new array with duplicate values removed, preserving insertion order.\n * Uses a Set internally for O(n) performance.\n */\nexport function unique<T>(arr: T[]): T[] {\n  return [...new Set(arr)];\n}\n",
      "tests/unique.test.ts": "import { test, expect } from \"bun:test\";\nimport { unique } from \"../src/unique\";\n\ntest(\"removes duplicate numbers\", () => {\n  expect(unique([1, 2, 2, 3, 1])).toEqual([1, 2, 3]);\n});\n\ntest(\"removes duplicate strings\", () => {\n  expect(unique([\"a\", \"b\", \"a\", \"c\"])).toEqual([\"a\", \"b\", \"c\"]);\n});\n\ntest(\"empty array returns empty\", () => {\n  expect(unique([])).toEqual([]);\n});\n\ntest(\"no duplicates unchanged\", () => {\n  expect(unique([1, 2, 3])).toEqual([1, 2, 3]);\n});\n\ntest(\"preserves insertion order\", () => {\n  expect(unique([3, 1, 2, 1, 3])).toEqual([3, 1, 2]);\n});\n",
      "package.json": "{\"name\":\"fixture\",\"type\":\"module\"}"
    }
  },
  "graders": [
    {
      "type": "deterministic_tests",
      "required": ["tests/unique.test.ts"],
      "command": "bun test"
    }
  ],
  "tracked_metrics": ["n_turns", "n_toolcalls", "n_total_tokens", "pass_at_1"],
  "tags": ["capability", "no-edit", "negative", "preserve-correctness"],
  "reference_solution": "capability/cap-already-implements_1/"
}
